name: evals
description: A simple command-line application.
version: 1.0.0
# repository: https://github.com/my_org/my_repo

environment:
  sdk: ^3.12.0-edge

dependencies:
  dart_mcp: ^0.5.0
  dart_mcp_server: ^0.1.0
  genkit: ^0.12.1
  genkit_google_genai: ^0.2.4
  genkit_mcp: ^0.1.4
  path: ^1.9.1
  schemantic: ^0.1.1

dev_dependencies:
  build_runner: ^2.10.5
  dart_flutter_team_lints: ^3.2.1
  test: ^1.25.15

dependency_overrides:
  dart_mcp:
    path: ../dart_mcp
  dart_mcp_server:
    path: ../dart_mcp_server
